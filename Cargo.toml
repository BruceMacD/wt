[package]
name = "worktree"
version = "0.1.0"
edition = "2021"
description = "Interactive Git worktree manager with fzf"
license = "MIT"
repository = "https://github.com/brucemacd/wt"

[dependencies]
clap = { version = "4", features = ["derive"] }
dirs = "5"
thiserror = "1"
